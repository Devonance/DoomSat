\begin{figure}[htbp]\centering\small\setlength{\parskip}{1pt}\setlength{\fboxsep}{5pt}\setlength{\fboxrule}{0.7pt}
\fcolorbox{black!50}{black!4}{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}\raggedright \textbf{Yamcs parameters (12 Hz), this tick}\par \texttt{CLEAR ahead 143, ahead-left 192, left 348, behind-left 400, behind 400, behind-right 400, right 284, ahead-right 328 (units)}\par \texttt{NEW ahead 100, ahead-left 255, left 100, behind-left 100, behind 100, behind-right 88, right 100, ahead-right 238}\par \texttt{DOOR all 0 except ahead-right (a door on that ray); AHEAD\_KIND NOTHING, EXIT\_DIST 0, STUCK false, ENEMY\_COUNT 0}\par \texttt{POS 1056,-3034, ANGLE 90, EXPLORED\_CELLS 19, LEVEL 1, KEYS 0}\end{minipage}}
\par\vspace{1pt}$\downarrow$\enspace\emph{telemetry $\rightarrow$ words: bands, novelty, one door field, one direction vocabulary; no number reaches jev}\par\vspace{1pt}
\fcolorbox{black!50}{black!4}{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}\raggedright \textbf{State document (code, no model). Only `sectors` is sent: nothing else is inspected this tick}\par \texttt{ahead: space open, ground new}\par \texttt{ahead-left: space open, ground never explored}\par \texttt{left / behind-left / behind / behind-right / right: space long, ground new}\par \texttt{ahead-right: space long, ground unknown, door mid-range}\par \texttt{every sector also carries exit\_here, key\_here, item\_here, hint\_here, tried\_recently (all no here)}\end{minipage}}
\par\vspace{1pt}$\downarrow$\enspace\emph{HTTPS POST /v1/systemone: the state plus one Score question per open direction, same rubric}\par\vspace{1pt}
\fcolorbox{black!50}{black!4}{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}\raggedright \textbf{jev, one request, 330 ms, 2452 input tokens (req\_01a0ca7e70fd7c8289c901853c0bd2d7)}\par \texttt{s\_ahead-left  2.98 (confidence 0.98)   \textless{}- the only never-explored direction}\par \texttt{s\_behind 2.06, s\_left 2.04, s\_behind-right 2.04, s\_behind-left 2.03}\par \texttt{s\_right 2.02, s\_ahead 2.01, s\_ahead-right 1.97}\par \texttt{the goal head was not asked this tick (every 10th); the danger head only when an enemy is in view}\end{minipage}}
\par\vspace{1pt}$\downarrow$\enspace\emph{eight scores on a four-level rubric, and a confidence each}\par\vspace{1pt}
\fcolorbox{black!50}{black!4}{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}\raggedright \textbf{Code rules}\par \texttt{top two 2.98 vs 2.06: a gap of 0.92 levels, well clear of unsure\_gap (0.20), so jev's ranking stands}\par \texttt{no commitment yet this episode, so no hysteresis to apply; commit the WORLD bearing 90+45 = 135 degrees}\par \texttt{ahead-left is not straight on: turn 45 degrees and do not walk this tick}\par \texttt{reflex: ammunition is zero so fire stays off; the turn is inside max\_turn\_deg; no turn is in flight}\end{minipage}}
\par\vspace{1pt}$\downarrow$\enspace\emph{one command per decision}\par\vspace{1pt}
\fcolorbox{black!50}{black!4}{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}\raggedright \textbf{CONTROL(move=0, strafe=0, turn=+45, fire=0, use=0, weapon=FIST meaning keep)}\par \texttt{Yamcs HTTP $\rightarrow$ CCSDS TC $\rightarrow$ UDP $\rightarrow$ F Prime CmdDispatcher $\rightarrow$ Doom component $\rightarrow$ payload turn setpoint (6 deg per tic)}\par \texttt{the next tick will not be judged until that turn has landed, so the sectors are never read mid-swing}\end{minipage}}
\caption{One real decision end to end (graph v1, jev-1.13.0, 22 September 2026): the numbers Yamcs delivered, the words code made from them, jev's probabilities, the code rules and the command that went up.}
\end{figure}
