\begin{figure}[htbp]\centering\small\setlength{\parskip}{1pt}\setlength{\fboxsep}{5pt}\setlength{\fboxrule}{0.7pt}
\fcolorbox{black!50}{black!4}{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}\raggedright \textbf{Payload telemetry through F Prime, CCSDS and Yamcs -- tic 3244, 92.7 s into the level}\par \texttt{health 33, armor 0, shotgun loaded; one Zombieman in view at mid-range}\par \texttt{eight candidate places, each with a world position, a path distance, how much unseen ground lies behind it, how wide the way on is and what is standing near it}\par \texttt{one of them is the level exit: the automap drew that line 166 units away, the first time this attempt has seen it}\par \texttt{the position and the distances are for CODE to aim and plan with; none of them reach the model}\end{minipage}}
\par\vspace{1pt}$\downarrow$\enspace\emph{telemetry $\rightarrow$ words: bands, comparisons and categories, one vocabulary. No coordinate, no number}\par\vspace{1pt}
\fcolorbox{black!50}{black!4}{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}\raggedright \textbf{State document (code, no model)}\par \texttt{here: mode explore, health critical, armor none, ammunition ready, keys none, stuck no}\par \texttt{needs: health urgent, ammo none, armor wanted}\par \texttt{combat: a straggler, Zombieman, one, mid-range}\par \texttt{t0 -- what the level exit, how\_far close, relative\_distance nearer than most, direction behind-left, unseen\_ground\_behind\_it none, tried\_before no, threat none}\par \texttt{t3 -- what unexplored edge, the\_way\_on\_is a doorway, unknown\_runs a fair way, gate none, further\_from\_the\_start about as far out, threat none}\end{minipage}}
\par\vspace{1pt}$\downarrow$\enspace\emph{HTTPS POST /v1/systemone: one call, eight target questions plus engage plus weapon}\par\vspace{1pt}
\fcolorbox{black!50}{black!4}{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}\raggedright \textbf{jev-1.13.0, one request, 354 ms, 5,232 input tokens (req\_01a0cdc29a2674a9abccb11b0b42b85d)}\par \texttt{target  t0 7.02 (the exit, confidence 0.56)  t3 5.60  t1 5.49  t2 5.34  t7 4.89  t5 4.64  t6 3.74  t4 1.03}\par \texttt{engage  Retreat (confidence 0.82)}\par \texttt{weapon  Shotgun (confidence 0.81)}\par \texttt{the need head is asked every goal\_every decisions, and was not asked on this one}\end{minipage}}
\par\vspace{1pt}$\downarrow$\enspace\emph{eight scores on a nine-level rubric, two choices, a confidence each}\par\vspace{1pt}
\fcolorbox{black!50}{black!4}{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}\raggedright \textbf{Code rules}\par \texttt{top two 7.02 against 5.60: a gap of 1.42 rubric levels, well clear of unsure\_gap, so jev's ranking stands and the exit is the target}\par \texttt{commitment: the margin to beat is 1.2 levels and nothing beats the exit, so it is committed to}\par \texttt{engage Retreat $\rightarrow$ mode RETREAT, stance retreat: back away from the Zombieman while going for it}\par \texttt{use\_at\_target is set, because the thing being walked to is something you open}\end{minipage}}
\par\vspace{1pt}$\downarrow$\enspace\emph{one INTENT per decision, with a time to live}\par\vspace{1pt}
\fcolorbox{black!50}{black!4}{\begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}\raggedright \textbf{INTENT(mode=RETREAT, target=2928,-4720, stance=retreat, fire=any attacker, use\_at\_target, ttl=1500ms)}\par \texttt{Yamcs HTTP $\rightarrow$ CCSDS TC $\rightarrow$ UDP $\rightarrow$ F Prime CmdDispatcher $\rightarrow$ Doom component $\rightarrow$ the onboard executor}\par \texttt{telemetry was 83 ms old, jev took 354 ms, the command took 44 ms: 481 ms from observation to effect, against a 900 ms budget}\par \texttt{the executor carries this out at 35 Hz for up to 1.5 s without asking anything -- the player does not stand still waiting for the next answer}\par \texttt{12.3 seconds later the level ended}\end{minipage}}
\caption{One real decision end to end, taken from the flight that finished E1M1 on 24 September 2026 (jev-1.13.0, tic 3244, the tic the exit first entered the candidate list): the numbers Yamcs delivered, the words code made from them, jev's probabilities, the code rules and the command that went up.}
\end{figure}
